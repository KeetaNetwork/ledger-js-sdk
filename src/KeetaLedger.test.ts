import { describe, it, expect } from 'vitest';
import { KeetaLedger } from './KeetaLedger';

describe('KeetaLedger', () => {
	it('should be importable', () => {
		expect(KeetaLedger).toBeDefined();
	});
});
